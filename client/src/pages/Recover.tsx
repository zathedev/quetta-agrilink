/** Market Desk recovery preview: generic confirmation mirrors the local PHP privacy model without simulating an external email service. */
import { type FormEvent, useState } from "react";
import { ArrowRight, ShieldCheck } from "lucide-react";
import { Brand } from "@/components/PreviewLayout";

export default function Recover() { const [requested, setRequested] = useState(false); const submit = (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); setRequested(true); }; return <div className="market-desk-auth"><header><Brand /><a href="/sign-in">Return to sign in</a></header><main className="desk-auth-layout"><section className="desk-auth-intro"><p className="desk-kicker">Local account recovery</p><h1>Request a secure return to your workspace.</h1><p>For the local XAMPP application, an administrator verifies identity and issues a one-time reset link through an approved offline process.</p><div className="desk-auth-points"><div><ShieldCheck size={19} /><span><strong>Privacy-first request</strong>The same confirmation appears whether or not a matching account exists.</span></div><div><ShieldCheck size={19} /><span><strong>One-time reset</strong>A verified administrator issues a 60-minute link that can only be used once.</span></div></div></section><section className="desk-auth-card"><p className="desk-kicker">Recovery request</p><h2>Need a new password?</h2>{requested ? <><div className="entry-confirm"><ShieldCheck size={17} /><span>If an active matching account exists, a local recovery request is ready for administrator review.</span></div><p className="desk-preview-note">Contact your Quetta AgriLink administrator through the approved local process. Never share a password in chat or email.</p><div className="desk-auth-footer"><span>Ready to return?</span><a href="/sign-in">Sign in</a></div></> : <><p>Enter the email address used for your account. The request does not expose whether an account exists.</p><form onSubmit={submit}><label>Account email address<input type="email" placeholder="name@company.pk" autoComplete="email" required /></label><button className="button button-canopy button-large" type="submit">Request local recovery <ArrowRight size={17} /></button></form><div className="desk-auth-footer"><span>Remembered your password?</span><a href="/sign-in">Return to sign in</a></div></>}</section></main></div>; }
