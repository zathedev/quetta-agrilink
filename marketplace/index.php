<?php
/** Orchard Ledger marketplace page: a filter rail and commodity-led results, never a decorative product grid. */
declare(strict_types=1);
require_once __DIR__ . '/../includes/bootstrap.php';
require_once __DIR__ . '/../includes/marketplace.php';

$filters = marketplace_filters($_GET);
$listings = find_listings($filters);
$categories = fetch_all('SELECT id, name FROM produce_categories WHERE is_active = 1 ORDER BY name');
$districts = fetch_all('SELECT DISTINCT district FROM locations ORDER BY district');
$pageTitle = 'Produce Marketplace';
$pageDescription = 'Search active agricultural produce listings from growers across Quetta and Balochistan.';
require __DIR__ . '/../includes/header.php';
?>
<section class="page-intro"><div class="site-container"><span class="eyebrow" style="color:var(--clay)">Produce marketplace</span><h1>Compare available produce before you call.</h1><p>Search listings by crop, origin, grade, expected price, and quantity. Every active entry records the information needed for a practical first conversation.</p></div></section>
<section class="section"><div class="site-container market-layout"><form class="filter-panel" method="get" data-marketplace-filter data-endpoint="<?= e(app_url('ajax/marketplace/search.php')) ?>"><h2>Refine availability</h2><div class="form-field"><label for="category_id">Product</label><select id="category_id" name="category_id"><option value="">All products</option><?php foreach ($categories as $category): ?><option value="<?= (int) $category['id'] ?>" <?= $filters['category_id'] === (int) $category['id'] ? 'selected' : '' ?>><?= e($category['name']) ?></option><?php endforeach; ?></select></div><div class="form-field"><label for="district">Location</label><select id="district" name="district"><option value="">All districts</option><?php foreach ($districts as $district): ?><option value="<?= e($district['district']) ?>" <?= $filters['district'] === $district['district'] ? 'selected' : '' ?>><?= e($district['district']) ?></option><?php endforeach; ?></select></div><div class="form-field"><label for="grade">Grade</label><select id="grade" name="grade"><option value="">Any grade</option><?php foreach (['A','B','C','Mixed'] as $grade): ?><option value="<?= $grade ?>" <?= $filters['grade'] === $grade ? 'selected' : '' ?>>Grade <?= $grade ?></option><?php endforeach; ?></select></div><div class="form-field"><label for="min_price">Minimum price (Rs./kg)</label><input id="min_price" name="min_price" type="number" min="1" step="0.01" value="<?= e((string) ($filters['min_price'] ?? '')) ?>"></div><div class="form-field"><label for="max_price">Maximum price (Rs./kg)</label><input id="max_price" name="max_price" type="number" min="1" step="0.01" value="<?= e((string) ($filters['max_price'] ?? '')) ?>"></div><div class="form-field"><label for="min_quantity">Minimum quantity (kg)</label><input id="min_quantity" name="min_quantity" type="number" min="1" step="0.01" value="<?= e((string) ($filters['min_quantity'] ?? '')) ?>"></div><div class="form-field"><label for="sort">Sort listings</label><select id="sort" name="sort"><option value="recent" <?= $filters['sort']==='recent'?'selected':'' ?>>Most recent</option><option value="price_low" <?= $filters['sort']==='price_low'?'selected':'' ?>>Price: low to high</option><option value="price_high" <?= $filters['sort']==='price_high'?'selected':'' ?>>Price: high to low</option><option value="quantity_high" <?= $filters['sort']==='quantity_high'?'selected':'' ?>>Highest quantity</option></select></div><button class="button button-primary" type="submit">Update results</button></form><div><div class="results-bar"><p data-marketplace-feedback><?= count($listings) ?> active listing<?= count($listings) === 1 ? '' : 's' ?> shown. Filtering updates without a page reload.</p><a class="button button-outline" href="<?= e(app_url('auth/register.php?role=farmer')) ?>">List your produce</a></div><div class="listing-grid" data-marketplace-results><?= listing_cards_html($listings) ?></div><p class="pagination-note">Marketplace pages are paginated as inventory grows. Current results show the most relevant active entries first.</p></div></div></section>
<?php require __DIR__ . '/../includes/footer.php'; ?>

